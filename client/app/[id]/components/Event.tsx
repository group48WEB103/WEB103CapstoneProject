import React from "react";

interface EventProps {
    event: any;
    BackgroundColor: string;
    AccentColor: string;
}

const Event: React.FC<EventProps> = ({ event, BackgroundColor, AccentColor }) => {
    return (
        <div>
            
        </div>
    )
}

export default Event;